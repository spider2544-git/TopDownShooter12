// Client Extraction Mode
// Currently identical to ClientTestMode - will be customized later
// Future: custom UI elements, wave indicators, different HUD

class ClientExtractionMode extends ClientBaseMode {
	// Uses all default behavior from ClientBaseMode for now
	// Ready for future customization without affecting other modes
}

window.ClientExtractionMode = ClientExtractionMode;



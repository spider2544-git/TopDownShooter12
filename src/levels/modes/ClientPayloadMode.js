// Client Payload Mode
// Currently identical to ClientTestMode - will be completely different later
// Future: cart rendering, progress bar, safe zone indicators

class ClientPayloadMode extends ClientBaseMode {
	// Uses all default behavior from ClientBaseMode for now
	// Will add cart rendering and UI elements later
}

window.ClientPayloadMode = ClientPayloadMode;



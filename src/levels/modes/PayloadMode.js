// Payload Mode
// Currently identical to TestMode - will be completely different later
// Future: cart mechanics, continuous enemy spawning, safe zones

const BaseGameMode = require('./BaseGameMode.js');

class PayloadMode extends BaseGameMode {
	// Uses all default behavior from BaseGameMode for now
	// Will add cart mechanics and different objectives later
}

module.exports = PayloadMode;


